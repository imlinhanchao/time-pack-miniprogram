<view class="read-container">
  <view class="top-container">
    <navigation-bar title="时间胶囊" back="{{true}}" showLogo="{{false}}" homeButton="{{true}}" color="white" background="#2487FF"></navigation-bar>
    <view class="title">{{packData.title}}</view>
  </view>

  <view class="container read-container">
    <view class="content">
      {{packData.content}}
    </view>
  </view>
  <view class="bottom-info">
    <view class="user-info">
      <image class="head-image" src="{{imgUrl + packData.create_avatar}}"></image>
      <view class="user-name">
        {{ packData.create_nick }}
        <text class="send-text" wx:if="{{packData.gift}}">赠送</text>
      </view>
    </view>
    <view class="pack-time">
      封装时间
      <text class="pack-time-text">{{oepnTime}}</text>
    </view>
  </view>
</view>